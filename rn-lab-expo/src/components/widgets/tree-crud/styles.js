import { StyleSheet } from 'react-native'

const TreeCrudStyles = StyleSheet.create({
    screenContainer: {
        flex: 1,
        backgroundColor: 'white',
    },
})

export default TreeCrudStyles